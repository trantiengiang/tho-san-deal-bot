# Local development overrides
# This file is safe to commit — contains no secrets
# Create .env file with actual values (see .env.example)

spring:
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true

logging:
  level:
    vn.thosandeal.bot: DEBUG
    org.springframework.web: DEBUG
    org.hibernate.SQL: DEBUG
